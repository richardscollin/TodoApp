# Data only container
datastore:
  image: busybox:latest
  volumes:
    - /data/db
  command: ["true"]

db:
  image: mongo:latest
  volumes_from:
    - datastore

web:
  build: .
  links:
    - db
  ports:
    - "80:3000"
  volumes:
    - .:/usr/src/app
